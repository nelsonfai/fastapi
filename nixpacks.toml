[phases.setup]
nixPkgs = [
  "python311",
  "tesseract5",
  "poppler_utils",
  "ghostscript",
  "gcc"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "hypercorn main:app --bind \"[::]:$PORT\""